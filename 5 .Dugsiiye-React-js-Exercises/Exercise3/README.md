# 1. Modify the *UserCard* component to accept *name* and *email* as props.

# 2. Update your *App.jsx* to pass different *name* and *email* values to *UserCard*.

# 3. *Render* multiple *UserCard* components with different user data.