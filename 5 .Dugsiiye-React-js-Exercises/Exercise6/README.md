# 1. Create a component called *GreetingTitle*.

# 2. It should have *two* pieces of state:

## *name* (default: `""`)

## *greeting* (default: `"Hello"`)

# 3. Whenever *name* or *greeting* changes, set the document title to *"{greeting}, {name}"*.

## If *name* is empty, show *"Welcome!"* instead.